// Package service holds business logic: status transitions, event
// recording, latest_status maintenance, and report assembly. The CLI
// and HTTP layers must reach the store only through this package.
package service
